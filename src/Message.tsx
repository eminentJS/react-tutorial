// PascalCasing: where every first letter is capitalized
function Message() {
  // jsx: javascript XML
  const name = '';
  if (name) return <h1>Hello {name}</h1>;
  return <h1>Hello World</h1>;
}

export default Message;
